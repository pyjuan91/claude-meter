{
  "name": "claude-meter",
  "version": "1.4.1",
  "private": true,
  "description": "Browser extension that displays Claude.ai usage limits as a floating widget.",
  "scripts": {
    "test": "vitest run",
    "test:ci": "vitest run --reporter=verbose",
    "test:watch": "vitest",
    "zip": "node scripts/zip.js",
    "zip:chrome": "node scripts/zip.js chrome",
    "zip:firefox": "node scripts/zip.js firefox"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
